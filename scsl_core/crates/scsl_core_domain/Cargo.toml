[package]
name = "scsl_core_domain"
edition.workspace = true
license.workspace = true
version.workspace = true

[lib]
name = "scsl_core_domain"
path = "src/lib.rs"

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }

[dev-dependencies]
serde_json = "1.0.149"
